# ai_project_index.toml
# AI-friendly codebase index for efficient token usage
# Generated by: https://github.com/keitheis/ai_project_index

[meta]
# High-level project overview
name = "{{PROJECT_NAME}}"
roots = [{{ROOT_FOLDERS}}]  # Root folders to analyze (e.g., ["src", "lib"] or ["."] for single root)
description = "TODO: Describe what this project does"
language = "TODO"          # e.g., Python, TypeScript, Go, Rust, Dart, Java, C++
framework = "TODO"         # e.g., Django, React, FastAPI, Flutter, Spring Boot, or "none"
architecture = "TODO"      # e.g., Monolith, Microservices, Clean Architecture, MVC, DDD

[structure]
# Document the folder organization
source = ""                # Main source folder (e.g., src/, lib/, app/)
modules = ""               # Features/modules folder (e.g., src/modules/, lib/features/)
tests = ""                 # Test folder (e.g., tests/, __tests__/, spec/)
config = ""                # Configuration files location
docs = ""                  # Documentation folder
scripts = ""               # Build/utility scripts

[modules]
# Reference to detailed module documentation
#
# Example:
# core = "modules/core.toml"
# auth = "modules/auth.toml"
# users = "modules/users.toml"
# payments = "modules/payments.toml"

[entry_points]
# Main files that start or configure the application
# main = ""                # Main entry file (e.g., main.py, index.ts, main.go)
# config = ""              # Main config file
# routes = ""              # API routes definition
# cli = ""                 # CLI entry point (if applicable)

[commands]
# Important commands for development workflow
# install = ""             # Install dependencies (e.g., npm install, pip install -r requirements.txt)
# build = ""               # Build the project
# dev = ""                 # Start development server
# test = ""                # Run tests
# lint = ""                # Run linter
# format = ""              # Format code
# deploy = ""              # Deploy command

# ──────────────────────────────────────────────
# Context Loading — Guide AI to the right files
# ──────────────────────────────────────────────

[context_loading]
# Help AI agents load ONLY relevant context for a given task.
# Think of this as a "routing table": task type → files to read.
#
# Example:
# [context_loading]
# on_bug_fix = [
#     "ai_project_index/modules/{affected_module}.toml",
#     "tests/",
# ]
# on_new_feature = [
#     "ai_project_index/ai_project_index.toml",
#     "ai_project_index/modules/{related_module}.toml",
#     "docs/architecture.md",
# ]
# on_refactor = [
#     "ai_project_index/ai_project_index.toml",
#     "ai_project_index/modules/",  # Read all module indices
# ]
# on_test_writing = [
#     "tests/conftest.py",
#     "tests/fixtures/",
# ]
# always_read = [
#     "ai_project_index/ai_project_index.toml",
# ]

# ──────────────────────────────────────────────
# Boundaries — What AI must NOT change or violate
# ──────────────────────────────────────────────

[boundaries]
# Architectural invariants the AI must respect.
# These are constraints that apply regardless of the task.
#
# Example:
# invariants = [
#     "Database access ONLY through repository layer",
#     "All external API calls go through src/clients/",
#     "Never import from internal modules across domain boundaries",
# ]
# protected_files = [
#     "src/core/config.py — Do not modify without explicit request",
#     "migrations/ — Never auto-generate; user handles migrations",
# ]
# protected_patterns = [
#     "Do not change public API signatures without discussion",
#     "Do not remove or rename exported types",
# ]

[dependencies.policy]
# Architectural rules for dependencies
allowed = [
    "modules -> core (one-way dependency)",
    "any -> shared utilities",
]
forbidden = [
    "core -> modules (core should not depend on specific modules)",
    "circular dependencies between modules",
]

[dependencies.external]
# Key third-party libraries and their purpose
#
# Example:
# "requests" = "HTTP client"
# "pydantic" = "Data validation"
# "react" = "UI framework"
# "express" = "Web server"

# ──────────────────────────────────────────────
# Testing — Help AI write correct tests
# ──────────────────────────────────────────────

[testing]
# How tests are organized and run in this project.
#
# Example:
# framework = "pytest"         # or jest, vitest, go test, etc.
# run_command = "pytest -x"    # Command to run all tests
# structure = "co-located"     # or "separate" (tests/ folder) or "co-located" (__tests__/ next to source)
# conventions = [
#     "Use fixtures from conftest.py, not inline setup",
#     "Mock external services, never hit real APIs in tests",
#     "Each module has its own test file",
#     "Name test files: test_{module}.py",
# ]
# coverage_command = ""        # e.g., "pytest --cov=src"

# ──────────────────────────────────────────────
# Conventions — Patterns AI should follow
# ──────────────────────────────────────────────

[conventions]
# Project-specific patterns and rules for AI to follow
#
# naming = "snake_case for functions, PascalCase for classes"
# imports = "absolute imports only"
# error_handling = "use Result type, avoid exceptions for flow control"

[conventions.patterns]
# Example patterns AI should follow:
# "Use dependency injection for services"
# "All API routes must have input validation"
# "Database access only through repository layer"

[conventions.files]
# Example file naming conventions:
# component = "*.component.tsx"
# service = "*.service.ts"
# test = "*.test.ts"
# model = "*_model.py"

# ──────────────────────────────────────────────
# Inline components (for smaller projects)
# ──────────────────────────────────────────────

# [[component]]
# For larger projects, move this to modules/core.toml
# name = "Logger"
# file = "src/utils/logger.ts"
# purpose = "Centralized logging utility"
# exports = ["logger", "setupLogger"]
# description = """
# Provides structured logging with levels (debug, info, warn, error).
# Automatically includes timestamps and context.
# """

# [[component]]
# name = "HttpClient"
# file = "src/utils/http.ts"
# purpose = "HTTP client wrapper with error handling"
# exports = ["httpClient", "apiRequest"]

[notes]
# Additional context for AI assistants
# Important information that doesn't fit elsewhere:
# "This project is in migration from X to Y"
# "Legacy code in old/ should not be modified"
# "Performance is critical — avoid N+1 queries"
